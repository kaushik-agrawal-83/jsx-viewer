# Phase 12 — Production Readiness

## Status
> 🔲 NOT_STARTED  
> Agent: update → 🔄 IN_PROGRESS → ✏️ CODE_COMPLETE → 🧪 TESTING_COMPLETE → ✅ COMPLETE

## Goal
Ship-ready: hardened error handling, structured logging, full README, clean `.dmg` build
with app icon + code signing instructions, and removal of all dev scaffolding from the
web prototype that doesn't belong in the native app.

## Demo Checkpoint
- [ ] Install fresh `.dmg` on a clean macOS machine → open app → drag `.jsx` → renders
- [ ] No console errors in production build
- [ ] `README.md` covers: web dev mode, Tauri dev mode, Tauri build, all requirements
- [ ] All Phase 0–11 UX states work correctly in the production build

---

## Tasks

### Error Handling Audit
- [ ] All `invoke()` calls in frontend have `.catch()` handlers — surface errors as toasts, not silent failures
- [ ] Rust `commands.rs`: all `?` operators have descriptive error messages (not `"error"`)
- [ ] `read_file` on binary file (not text) → error message: "File is not valid UTF-8 text"
- [ ] `watch_file` called twice on same path → idempotent (no duplicate watchers)
- [ ] File deleted while being watched → emit `"file-deleted"` event → tab shows `missing` status
- [ ] App closes → all `RecommendedWatcher` instances dropped (verify no zombie processes)

### `"file-deleted"` Event (new)
- [ ] `frontend/src-tauri/src/commands.rs` — in notify handler: if `EventKind::Remove(_)` → emit `"file-deleted"` with `{ path }`
- [ ] `frontend/src/hooks/useFileWatcher.ts` — listen for `"file-deleted"` → call `onDeleted` callback if path matches
- [ ] `useTabs` — `onDeleted(path)` → set matching tabs to `status: 'missing'`
- [ ] `Preview.tsx` — `missing` status shows `<MissingFile>` component

### Logging
- [ ] Rust: add `log` + `env_logger` crate; log at `info` level: file open, watch start/stop, errors
- [ ] Frontend: production build strips all `console.log` via Vite `esbuild.drop: ['console', 'debugger']` in `vite.config.ts` production mode

### Dev Artifact Cleanup
- [ ] Remove `DevSandbox.tsx` entirely (was Phase 4 temp UI) — confirm it's already behind DEV flag or delete
- [ ] Remove any `TODO` comments left from earlier phases (search and confirm or resolve)
- [ ] Remove `docs/WEB-LIMITATIONS.md` if all limitations are resolved in Tauri
- [ ] Confirm `docker-compose.yml` and `backend/` are documented clearly as "web dev only" in README

### Tauri Build Config
- [ ] `frontend/src-tauri/tauri.conf.json` — production settings:
  - `"devUrl"` removed (only used in dev)
  - Bundle identifier: `com.jsx-viewer.app`
  - App version: `0.1.0`
  - Category: `DeveloperTool`
  - Copyright: `© 2026 JSX Viewer`
- [ ] App icon finalized: `frontend/src-tauri/icons/` contains all sizes (generated by `npx tauri icon`)
- [ ] Code signing: document steps in README (requires Apple Developer account + `APPLE_SIGNING_IDENTITY` env var); build still works unsigned for local use

### README
- [ ] `README.md` in project root covers:
  - What JSX Viewer does (2 sentences)
  - Supported JSX libraries (import table from DESIGN.md §11)
  - **Web dev mode**: prerequisites (Docker), `docker-compose up`, open `http://localhost:5173`
  - **Tauri dev mode**: prerequisites (Rust, Node.js 20), `cd frontend && npm install && npm run tauri dev`
  - **Build `.dmg`**: `cd frontend && npm run tauri build` → output path
  - Code signing note
  - Known limitations (browser File System Access API required for watching in web mode)

### Final Test Pass
- [ ] Run all tests: `cd frontend && npm test && cd src-tauri && cargo test`
- [ ] Zero TypeScript errors: `cd frontend && npx tsc --noEmit`
- [ ] Zero Rust warnings: `cd frontend/src-tauri && cargo clippy -- -D warnings`
- [ ] Zero lint errors: `cd frontend && npm run lint`
- [ ] Production Vite build succeeds: `cd frontend && npm run build`
- [ ] `npm run tauri build` succeeds and produces `.dmg`

---

## Testing Requirements

### All existing tests must still pass
- [ ] `cd frontend && npm test` — all Vitest tests green
- [ ] `cd frontend/src-tauri && cargo test` — all Rust tests green

### New Tests
- [ ] `watch_file` called twice on same path → only one watcher registered
- [ ] File deleted while watched → `"file-deleted"` event emitted
- [ ] `read_file` on binary file → returns `Err` (not panic)

### Manual — Full Smoke Test on Clean Install
- [ ] Fresh macOS → install `.dmg` → open app → drag `.jsx` → renders ✓
- [ ] Tab, split view, watcher, sidebar collapse all work ✓
- [ ] Open With in Finder works ✓
- [ ] Uninstall app → no leftover processes or daemon files

---

## Done Gate
- [ ] All tests passing (Vitest + Rust)
- [ ] Zero TypeScript errors, zero Rust warnings, zero lint errors
- [ ] Production Vite build succeeds
- [ ] `.dmg` produced and installs on clean macOS
- [ ] README complete and accurate
- [ ] No console errors in production (Tauri) app
- [ ] File-deleted handling works
- [ ] All dev scaffolding removed or clearly documented

## Dependencies
- **Requires complete:** Phase 11 — macOS File Association
- **Enables:** Ship 🚀 (Phase 13 — File Watcher is optional, can be added post-ship)

## Notes
- Code signing is optional for personal use. For distribution via GitHub Releases, signing + notarization is required by macOS Gatekeeper. Document but don't block the done gate on it.
- `cargo clippy -- -D warnings` may surface issues from earlier phases. Fix all warnings — they are real issues in a safety-first language.
- Version `0.1.0` in `tauri.conf.json` should match `frontend/package.json` `version`. Keep them in sync.
